# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080  # порт внутри kind (NodePort сервиса)
        hostPort: 8080        # порт на хост-машине
        protocol: TCP